# Boks Base

Placeholder tecnico per validare orientamento e rotazioni.

Asset:

- `base-right.svg`: quadrato + freccia verso destra

Le altre direzioni sono ottenute via `transformFallback` nel manifest:

- `left` -> `mirror-x`
- `up` -> `rotate-left`
- `down` -> `rotate-right`
